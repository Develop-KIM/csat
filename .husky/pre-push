#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

if git diff --name-only @{upstream}..HEAD | grep -q "^backend/"; then
  npm test || { echo "테스트 실패"; exit 1; }
  echo "테스트 통과"
else
  echo "백엔드 변경사항 없음"
fi
